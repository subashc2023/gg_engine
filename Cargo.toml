[workspace]
members = [
    "gg_engine",
    "gg_editor",
    "gg_sandbox",
]
resolver = "2"

# Debug
[profile.dev]
opt-level = 0
debug = true
overflow-checks = true

# Release
[profile.release]
opt-level = 3
debug = false
lto = true
codegen-units = 1
strip = true
